# G-GRAPH-2: layer backbone present (>=25 PML nodes, >=80 linked tickers)

- status: open
- prediction: backbone persists and grows
- falsifier: backbone removed/shrunk (import regressed)
- data: graph_v2.json PML_ nodes + suppliers[]
